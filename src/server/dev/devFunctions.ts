// import * as faker from '@faker-js/faker'
// import { db } from '../db';

export const getDataAndMorph = async function(){
    // let data = faker.en.commerce?.product_name
    // let mySet: Set<string> = new Set();

    // while(mySet.size<=100){
    //     let categ = data!.material[Math.floor(Math.random()*data!.material.length)]
    //                 + ' ' 
    //                 + data!.product[Math.floor(Math.random()*data!.product.length)];
    //     mySet.add(categ);
    // }

    // let myArr:string[] = Array.from(mySet);
    // type catEntryType = {
    //     name: string,
    //     id?: number
    // }
    // let myDataArr:catEntryType[] = []
    // for(let i=0; i<100; i++){
    //     myDataArr.push({
    //         name: myArr[i]!,
    //         id: i+1
    //     })
    // }

    // // const dbData = await db.categories.createMany({
    // //     data: myDataArr
    // // })
    // const dbData = await db.categories.findMany()
    // console.log(dbData)


    // await db.categories.deleteMany()
    // console.log(myDataArr)
}